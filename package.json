{
  "name": "github-trend-radar",
  "version": "1.0.0",
  "private": true,
  "description": "Daily GitHub Trending reports generated by GitHub Actions",
  "type": "module",
  "scripts": {
    "report": "node src/generate-report.mjs",
    "report:local": "node --env-file=.env src/generate-report.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
