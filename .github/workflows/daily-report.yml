name: Generate GitHub trend report

on:
  schedule:
    # GitHub Actions schedules use UTC. 01:05 UTC is 09:05 in Asia/Shanghai.
    # Avoiding minute 00 reduces the chance of GitHub scheduler congestion.
    - cron: "5 1 * * *"
  workflow_dispatch:
    inputs:
      report_date:
        description: "Report date in YYYY-MM-DD (optional; defaults to today in Asia/Shanghai)"
        required: false
        type: string

permissions:
  contents: write

concurrency:
  group: github-trend-radar
  cancel-in-progress: false

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      TZ: Asia/Shanghai
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      MINIMAX_API_KEY: ${{ secrets.MINIMAX_API_KEY }}
      MINIMAX_MODEL: ${{ vars.MINIMAX_MODEL || 'MiniMax-M2.7' }}
      MINIMAX_BASE_URL: ${{ vars.MINIMAX_BASE_URL || 'https://api.minimaxi.com/v1' }}
      REPORT_DATE: ${{ inputs.report_date }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Generate report
        run: npm run report

      - name: Commit report
        run: |
          if git diff --quiet -- reports README.md; then
            echo "No report changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports README.md
          git commit -m "docs: add GitHub trend report for $(date +%F)"
          for attempt in 1 2 3; do
            if git push origin HEAD:main; then
              exit 0
            fi
            echo "Push race detected; rebasing and retrying (${attempt}/3)."
            git fetch origin main
            git rebase origin/main
          done
          echo "Unable to push after three attempts."
          exit 1
